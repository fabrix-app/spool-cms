'use strict'
/* global describe, it */
const assert = require('assert')

describe('RouterRepoService', () => {
  it('should exist', () => {
    assert(global.app.api.services['RouterRepoService'])
  })
})
