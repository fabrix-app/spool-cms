'use strict'
/* global describe, it */
const assert = require('assert')

describe('RouterDBService', () => {
  it('should exist', () => {
    assert(global.app.api.services['RouterDBService'])
  })
})
