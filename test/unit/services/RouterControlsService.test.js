'use strict'
/* global describe, it */
const assert = require('assert')

describe('RouterControlsService', () => {
  it('should exist', () => {
    assert(global.app.api.services['RouterControlsService'])
  })
})
